<h1><%= @game.id %></h1>


<% if current_user.id == @game.user_id %>
  <p>host</p>
  <div id="buzzs" data-game-id="<%= @game.id %>">
    <% @game.buzzs.each do |buzz| %>
      <div class="buzz-container" id="buzz-<%= buzz.id %>">
        <i class="author">
          <span><%= buzz.user.nickname %></span>
          <small><%= buzz.created_at.strftime("%k:%M:%S") %></small>
        </i>
      </div>
    <% end %>
    <button style="display: none" id="buzzer">buzz</button>
  </div>
    <%= link_to "Clear buzzs", clearbuzzs_path(@buzzs, game_id: @game.id) %>





<% else %>
  <p>player</p>
  <%= simple_form_for [ @game, @buzz ], remote: true do |f| %>
    <%= f.input :buzzed, as: :hidden %>
  <% end %>
  <button id="buzzer">buzz</button>
<% end %>






